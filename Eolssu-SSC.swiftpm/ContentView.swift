import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("전통가락")
                .font(.myFont(size: 120))
            Text("This is Korean Traditional Rythm")
                .font(.myFont(size: 40))
        }
    }
}
